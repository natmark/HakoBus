language: objective-c
osx_image: xcode8.2

before_install:
    - brew update
    - brew install carthage
    - carthage update --platform ios

script:
    - xcodebuild -scheme HakoBus -configuration Debug clean build test

branches:
      only:
        - master
        - develop
